{{> nav-bar }}


<div class="container center-align">

<h1 class="">{{common_name}}</h1>
<h5>{{points}} points</h5>
<img alt="image of {{common_name}}" src="{{image_URL}}" class="z-depth-3" id="single-bird-img" />

<div class="col s12 l12 xl4 action-buttons">
        <a href="/birds"><button class="btn-large btn btn-highlight ">Go back to all birds</button></a>
      </div>

</div>